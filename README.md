# GUANAS RUE
Collection of scripts to analyse newborns data

## First steps:
### Create a python environment
- Download and install conda to manage python environments
- create a new environment (Python 3.10 for stability):
  ```bash
  conda install -n envname python=3.10
  ```
